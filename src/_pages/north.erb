---
layout: shop
---

<div class="max-w-6xl mx-auto px-2 py-4">
  <img src="<%= relative_url '/images/north-cover.png' %>" alt="home banner" />
</div>

<%= render Shared::Callshop.new(metadata: site.metadata, resource: resource, shop: site.metadata.north) %>
<%= render Shared::Shophours.new(metadata: site.metadata, resource: resource, shop: site.metadata.north) %>

<%= render Shared::ContactMap.new(metadata: site.metadata, resource: resource, shop: site.metadata.north) %>
<%= render Shared::Footericons.new(metadata: site.metadata, resource: resource, shop: site.metadata.north, icons: true) %>
