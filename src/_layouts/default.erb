<!doctype html>
<html lang="<%= site.locale %>">
  <head>
    <%= render "head", metadata: site.metadata, title: resource.data.title %>
  </head>
  <body class="font-serif antialised bg-flesh text-maxx-black lining-nums <%= resource.data.layout %> <%= resource.data.page_class %>">

    <main>
      <%= yield %>
    </main>

  </body>
</html>
